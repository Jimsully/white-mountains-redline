# First Codex Prompt

Use this prompt in Codex after opening the repository:

> Read `AGENTS.md` and all files in `docs/`. Treat them as product requirements. Implement the next Milestone 1 slice without changing the product's legal/data guardrails. First, refactor trail data access behind a typed repository interface that supports the current demo dataset and a future Supabase adapter. Then add trail-name search and region filtering to the redline map. Add tests for progress calculations and filtering. Do not invent real trail geometry or label any data verified. Keep the current UI working. Run typecheck/lint/tests available in the repository and summarize changed files and remaining blockers.
